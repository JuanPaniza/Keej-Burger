/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com'], // Agrega aquí el dominio de Cloudinary u otros dominios de imágenes que vayas a utilizar
      },
}

module.exports = nextConfig

