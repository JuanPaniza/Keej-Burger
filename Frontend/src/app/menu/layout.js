import "../globals.css"

export const metadata = {
  title: 'KB | Menu',
  description: 'Keej Burguer',
  charset: "UTF-8"
}

export default function MenuLayout({ children }) {
  return (
    <>
      {children}
    </>
  )
}
